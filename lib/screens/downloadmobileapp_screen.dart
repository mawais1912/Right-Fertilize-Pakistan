import 'package:flutter/material.dart';


import '../widgets/base_screen.dart';

class DownloadMobileAppScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BaseScreen(

      body: Center(
        child: Text('DownloadMobileAppScreen.'),
      ),
    );
  }
}
