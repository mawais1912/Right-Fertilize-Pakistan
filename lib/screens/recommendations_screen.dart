import 'package:flutter/material.dart';

import '../widgets/base_screen.dart';


class RecommendationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BaseScreen(

      body: Center(
        child: Text('Recommendations Screen will be here.'),
      ),
    );
  }
}
